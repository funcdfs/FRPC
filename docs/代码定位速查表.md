# FRPC 代码定位速查表

快速找到面试时需要讲解的关键代码。

---

## 一、按简历内容定位

### 1. "基于 Reactor 模型自研非阻塞网络库"

**核心文件**:
```
include/frpc/network_engine.h       (第 20-80 行)
src/network_engine.cpp              (第 50-150 行)
```

**关键函数**:
- `NetworkEngine::run()` - 事件循环主逻辑
- `NetworkEngine::register_read_event()` - 注册读事件
- `NetworkEngine::register_write_event()` - 注册写事件

**演示命令**:
```bash
# 查看网络引擎接口
cat include/frpc/network_engine.h | grep -A 20 "class NetworkEngine"

# 查看事件循环实现
cat src/network_engine.cpp | grep -A 50 "void NetworkEngine::run()"
```

---

### 2. "深度集成 C++20 无栈协程机制"

**核心文件**:
```
include/frpc/coroutine.h            (第 30-100 行)
include/frpc/network_awaitable.h    (第 20-80 行)
src/network_awaitable.cpp           (第 40-120 行)
```

**关键类型**:
- `RpcTaskPromise` - Promise Type 定义
- `SendAwaitable` - 异步发送的 Awaitable 对象
- `RecvAwaitable` - 异步接收的 Awaitable 对象

**演示命令**:
```bash
# 查看 Promise Type
cat include/frpc/coroutine.h | grep -A 30 "struct RpcTaskPromise"

# 查看 Awaitable 对象
cat include/frpc/network_awaitable.h | grep -A 40 "struct SendAwaitable"
```

---

### 3. "通过封装 co_await 将异步事件回调转化为同步编写范式"

**核心文件**:
```
include/frpc/network_awaitable.h    (完整文件)
examples/complete_rpc_example.cpp   (第 50-100 行)
```

**对比代码**:
```bash
# 查看协程方式的代码
cat examples/complete_rpc_example.cpp | grep -A 20 "RpcTask<"

# 查看 Awaitable 的三个关键方法
cat include/frpc/network_awaitable.h | grep -E "(await_ready|await_suspend|await_resume)"
```

**关键点**:
- `await_ready()` - 检查是否已完成
- `await_suspend()` - 挂起协程，注册回调
- `await_resume()` - 恢复后获取结果

---

### 4. "独立实现轻量级协程调度器"

**核心文件**:
```
include/frpc/coroutine_scheduler.h  (第 30-100 行)
src/coroutine_scheduler.cpp         (第 50-200 行)
```

**关键函数**:
- `CoroutineScheduler::create_coroutine()` - 创建协程
- `CoroutineScheduler::suspend()` - 挂起协程
- `CoroutineScheduler::resume()` - 恢复协程
- `CoroutineScheduler::destroy()` - 销毁协程

**演示命令**:
```bash
# 查看调度器接口
cat include/frpc/coroutine_scheduler.h | grep -A 50 "class CoroutineScheduler"

# 查看协程状态定义
cat include/frpc/coroutine_scheduler.h | grep -A 10 "enum class CoroutineState"
```

---

### 5. "通过定制 promise_type 与接管内存分配逻辑"

**核心文件**:
```
include/frpc/coroutine.h            (第 50-80 行)
include/frpc/memory_pool.h          (第 20-80 行)
src/memory_pool.cpp                 (第 30-100 行)
```

**关键代码**:
```bash
# 查看自定义内存分配
cat include/frpc/coroutine.h | grep -A 10 "operator new"

# 查看内存池实现
cat include/frpc/memory_pool.h | grep -A 30 "class MemoryPool"
```

**关键点**:
- `operator new()` - 从内存池分配
- `operator delete()` - 归还到内存池
- 空闲链表管理

---

### 6. "规避了默认的堆内存分配开销"

**性能数据位置**:
```
benchmarks/BENCHMARK_RESULTS.md     (完整文件)
benchmarks/benchmark_coroutine.cpp  (第 30-80 行)
```

**查看命令**:
```bash
# 查看性能测试结果
cat benchmarks/BENCHMARK_RESULTS.md

# 查看协程性能测试代码
cat benchmarks/benchmark_coroutine.cpp | grep -A 20 "BM_CoroutineCreation"
```

**关键数据**:
- 默认堆分配: ~100ns/次
- 内存池分配: ~10ns/次
- 性能提升: 10 倍
- 减少堆分配: 80%

---

### 7. "并发压测场景下（结合 wrk / jmeter）"

**测试文件**:
```
stress_tests/stress_test_server.cpp (完整文件)
stress_tests/run_all_tests.sh       (完整文件)
stress_tests/README.md               (完整文件)
```

**查看命令**:
```bash
# 查看压力测试脚本
cat stress_tests/run_all_tests.sh

# 查看测试说明
cat stress_tests/README.md

# 查看测试服务器
cat stress_tests/stress_test_server.cpp | head -50
```

**测试结果**:
- QPS: 52,000
- P99 延迟: 8.7ms
- 并发连接: 10,000+

---

### 8. "集成动态服务发现与健康检测机制"

**核心文件**:
```
include/frpc/service_registry.h     (第 30-100 行)
src/service_registry.cpp            (第 50-150 行)
include/frpc/health_checker.h       (第 30-80 行)
src/health_checker.cpp              (第 50-200 行)
```

**关键函数**:
```bash
# 服务注册
cat include/frpc/service_registry.h | grep -A 10 "register_service"

# 健康检测
cat include/frpc/health_checker.h | grep -A 20 "check_loop"
```

---

### 9. "实现内置连接池的 RPC 客户端"

**核心文件**:
```
include/frpc/connection_pool.h      (第 30-100 行)
src/connection_pool.cpp             (第 50-200 行)
include/frpc/rpc_client.h           (第 30-80 行)
src/rpc_client.cpp                  (第 50-150 行)
```

**关键函数**:
```bash
# 连接池获取连接
cat include/frpc/connection_pool.h | grep -A 20 "get_connection"

# RPC 客户端调用
cat include/frpc/rpc_client.h | grep -A 30 "template.*call"
```

---

### 10. "采用负载均衡算法进行流量调度"

**核心文件**:
```
include/frpc/load_balancer.h        (完整文件)
src/load_balancer.cpp               (完整文件)
```

**查看命令**:
```bash
# 查看所有负载均衡策略
cat include/frpc/load_balancer.h

# 查看轮询实现
cat src/load_balancer.cpp | grep -A 20 "RoundRobinLoadBalancer::select"

# 查看加权轮询实现
cat src/load_balancer.cpp | grep -A 30 "WeightedRoundRobinLoadBalancer::select"
```

**支持的策略**:
- 轮询 (Round Robin)
- 随机 (Random)
- 最少连接 (Least Connection)
- 加权轮询 (Weighted Round Robin)

---

### 11. "有效降低 TCP 握手机制带来的额外开销"

**核心文件**:
```
include/frpc/connection_pool.h      (第 50-100 行)
src/connection_pool.cpp             (第 100-200 行)
```

**关键逻辑**:
```bash
# 查看连接复用逻辑
cat src/connection_pool.cpp | grep -A 30 "get_connection"

# 查看连接归还逻辑
cat src/connection_pool.cpp | grep -A 20 "return_connection"
```

**性能数据**:
- TCP 握手开销: 1-3ms
- 连接复用率: >90%
- 显著降低延迟

---

## 二、按技术点定位

### Reactor 模式

**文件**: `src/network_engine.cpp`
**行数**: 50-150
**关键词**: `epoll_wait`, `EPOLLIN`, `EPOLLOUT`

```bash
cat src/network_engine.cpp | grep -A 50 "void NetworkEngine::run()"
```

---

### C++20 协程

**文件**: `include/frpc/coroutine.h`
**行数**: 30-100
**关键词**: `promise_type`, `co_await`, `co_return`

```bash
cat include/frpc/coroutine.h | grep -A 50 "struct RpcTaskPromise"
```

---

### Awaitable 对象

**文件**: `include/frpc/network_awaitable.h`
**行数**: 20-80
**关键词**: `await_ready`, `await_suspend`, `await_resume`

```bash
cat include/frpc/network_awaitable.h | grep -A 40 "struct SendAwaitable"
```

---

### 内存池

**文件**: `include/frpc/memory_pool.h`
**行数**: 20-80
**关键词**: `allocate`, `deallocate`, `free_list`

```bash
cat include/frpc/memory_pool.h | grep -A 30 "class MemoryPool"
```

---

### 协程调度器

**文件**: `src/coroutine_scheduler.cpp`
**行数**: 50-200
**关键词**: `create`, `suspend`, `resume`, `destroy`

```bash
cat src/coroutine_scheduler.cpp | grep -A 30 "void CoroutineScheduler::resume"
```

---

### 服务注册

**文件**: `src/service_registry.cpp`
**行数**: 50-150
**关键词**: `register_service`, `get_instances`, `subscribe`

```bash
cat src/service_registry.cpp | grep -A 20 "bool ServiceRegistry::register_service"
```

---

### 健康检测

**文件**: `src/health_checker.cpp`
**行数**: 50-200
**关键词**: `check_loop`, `check_instance`, `consecutive_failures`

```bash
cat src/health_checker.cpp | grep -A 50 "RpcTask<void> HealthChecker::check_loop"
```

---

### 负载均衡

**文件**: `src/load_balancer.cpp`
**行数**: 30-150
**关键词**: `select`, `RoundRobin`, `Weighted`

```bash
cat src/load_balancer.cpp | grep -A 20 "ServiceInstance.*::select"
```

---

### 连接池

**文件**: `src/connection_pool.cpp`
**行数**: 50-200
**关键词**: `get_connection`, `return_connection`, `idle_connections`

```bash
cat src/connection_pool.cpp | grep -A 30 "RpcTask<Connection> ConnectionPool::get_connection"
```

---

## 三、快速演示脚本

### 1. 展示网络引擎

```bash
# 查看接口定义
cat include/frpc/network_engine.h | head -100

# 查看事件循环
cat src/network_engine.cpp | grep -A 50 "void NetworkEngine::run()"

# 查看事件注册
cat src/network_engine.cpp | grep -A 20 "register_read_event"
```

---

### 2. 展示协程机制

```bash
# 查看 Promise Type
cat include/frpc/coroutine.h | grep -A 50 "struct RpcTaskPromise"

# 查看 Awaitable 对象
cat include/frpc/network_awaitable.h | grep -A 40 "struct SendAwaitable"

# 查看用户代码示例
cat examples/complete_rpc_example.cpp | grep -A 30 "RpcTask<"
```

---

### 3. 展示内存优化

```bash
# 查看自定义内存分配
cat include/frpc/coroutine.h | grep -A 10 "operator new"

# 查看内存池实现
cat include/frpc/memory_pool.h | grep -A 30 "class MemoryPool"

# 查看性能数据
cat benchmarks/BENCHMARK_RESULTS.md | grep -A 10 "Memory"
```

---

### 4. 展示服务治理

```bash
# 服务注册
cat include/frpc/service_registry.h | grep -A 20 "class ServiceRegistry"

# 健康检测
cat include/frpc/health_checker.h | grep -A 20 "class HealthChecker"

# 负载均衡
cat include/frpc/load_balancer.h | grep -A 30 "class.*LoadBalancer"

# 连接池
cat include/frpc/connection_pool.h | grep -A 20 "class ConnectionPool"
```

---

### 5. 运行完整示例

```bash
# 编译
mkdir -p build && cd build
cmake .. && make

# 运行服务端
./examples/rpc_server_example &

# 运行客户端
./examples/rpc_client_example

# 查看结果
```

---

## 四、面试现场快速查找

### 使用 grep 快速定位

```bash
# 查找 Reactor 相关代码
grep -r "epoll_wait" src/

# 查找协程相关代码
grep -r "co_await" include/

# 查找内存池相关代码
grep -r "MemoryPool" include/ src/

# 查找服务注册相关代码
grep -r "register_service" include/ src/

# 查找负载均衡相关代码
grep -r "LoadBalancer" include/ src/
```

---

### 使用 find 快速定位文件

```bash
# 查找所有头文件
find include/ -name "*.h"

# 查找所有实现文件
find src/ -name "*.cpp"

# 查找所有示例
find examples/ -name "*.cpp"

# 查找所有测试
find tests/ -name "test_*.cpp"
```

---

### 使用 wc 统计代码量

```bash
# 统计总代码量
find include/ src/ -name "*.h" -o -name "*.cpp" | xargs wc -l

# 统计核心组件代码量
wc -l include/frpc/*.h src/*.cpp

# 统计测试代码量
wc -l tests/*.cpp
```

---

## 五、关键数据速记

### 性能指标

```
QPS: 52,000
P50 延迟: 2.1ms
P99 延迟: 8.7ms
P999 延迟: 15.3ms
并发连接: 10,000+
协程切换: 12ns
内存分配: 8ns (内存池)
连接复用率: >90%
```

### 代码规模

```
总代码量: ~15,000 行
头文件: ~5,000 行
实现文件: ~8,000 行
测试代码: ~2,000 行
```

### 组件数量

```
核心组件: 9 个
负载均衡策略: 4 种
错误码: 20+ 个
配置参数: 30+ 个
```

---

**提示**: 面试前建议在本地运行一遍所有示例和测试，确保熟悉代码结构和运行流程。
